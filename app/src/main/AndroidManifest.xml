<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- MQTT 服务权限 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp"
        android:usesCleartextTraffic="true">

        <!-- MQTT Service -->
        <service android:name="org.eclipse.paho.android.service.MqttService" />

        <!-- 启动页（入口） -->
        <activity
            android:name=".presentation.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MyApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 登录页 -->
        <activity
            android:name=".presentation.auth.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 注册页 -->
        <activity
            android:name=".presentation.auth.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 主页 -->
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="false" />

        <!-- 添加设备页 -->
        <activity
            android:name=".presentation.device.AddDeviceActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 设备控制详情页 -->
        <activity
            android:name=".presentation.device.DeviceControlActivity"
            android:exported="false" />

        <!-- 快速设备创建工具（调试用） -->
        <activity
            android:name=".ui.debug.QuickDeviceCreatorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 规则编辑器 -->
        <activity
            android:name=".ui.rule.RuleEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 网络状态监听器 -->
        <receiver
            android:name=".util.NetworkStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>

